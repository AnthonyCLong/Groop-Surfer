public enum MoveState {
    Default,
    TopSurf,
    LeftSurf,
    RightSurf
}

public enum Direction {
    Left,
    Right,
    Forward,
    Backward,
    Up,
    Down
}

public enum RampType {
    Straight,
    Bend
}
